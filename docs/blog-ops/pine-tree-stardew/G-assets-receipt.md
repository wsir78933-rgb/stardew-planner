# G-ASSETS receipt: `pine-tree-stardew`

**Original generation date:** 2026-09-22
**Reconciliation:** read-only comparison of the six current media files and refreshed EN/ZH handoffs; no asset bytes or other files were written.
**Scope:** media receipt only; refreshed handoffs were read only; no article, source, registry, route, or test files changed by this reconciliation.
**Status:** `PASS_LOCAL_RECEIPT` for six-file presence, formats, dimensions, bytes, SHA-256 values, and localized cover-alt binding; page assembly remains blocked by unresolved registry metadata and source/page/runtime state.

## Approved media set

The approved set is one shared cover plus two shared in-article teaching figures. All six files are original, locally drawn vector-style artwork with no gameplay screenshot, copied project sprite, embedded language label, fixed growth countdown, profit ranking, or planner UI. Because the artwork contains no locale-specific text, the same WebP/AVIF pair is intended for both `en` and `zh-CN`; the page layer must still use the localized handoff alt/caption strings below.

| Role | WebP path | AVIF path | Dimensions | WebP bytes / budget | AVIF bytes | WebP SHA-256 | AVIF SHA-256 |
|---|---|---|---:|---:|---:|---|---|
| Cover | `public/blog/pine-tree-stardew-cover.webp` | `public/blog/pine-tree-stardew-cover.avif` | `1672×941` | `32,094` / `≤1,310,720` | `19,430` | `4d559005a41e1b64713fb7bf6609a8427bdfcab16c0e48edc851a30545fc6517` | `6c5127c0859770020f24afb0b803e8a22fac923a65ca575b8eec73d614a2b0a1` |
| Figure 1 | `public/blog/illustrations/pine-tree-seed-to-tar.webp` | `public/blog/illustrations/pine-tree-seed-to-tar.avif` | `1672×941` | `23,214` / `≤409,600` | `17,100` | `42a85ee5c0f45fb4d25a255f0923953ee5df4dfe4bfc2de4cb5b3aee9200488d` | `bd14f983d87b9eac3ae3e1fe4a8950a15bfd0b86317edd9eafa7310165826544` |
| Figure 2 | `public/blog/illustrations/pine-tree-stage-four-neighbor.webp` | `public/blog/illustrations/pine-tree-stage-four-neighbor.avif` | `1672×941` | `20,746` / `≤409,600` | `15,994` | `1fa68fda236109cdd3221cecede4714a99f900c62a24b4771f046e910d44f2b5` | `60c95369e3965b51137401600c0b7f33ea086586db8ebb3ebe9d2d02c8b4b3f0` |

## Handoff intent binding

### Cover

- Visual intent: a recognizable mature Pine Tree landscape with a visible Pine Cone, Tapper, and amber Pine Tar output; theme recognition only, not a teaching replacement for the body figures.
- Caption: none intended by `A-media-plan.md`.
- Current EN handoff cover alt: `Illustrated Pine Tree landscape with a Tapper and amber Pine Tar on a mature Pine, plus Pine Cones in the foreground.`
- Current ZH handoff cover alt: `松树山林插画：成熟松树上可见树液采集器和琥珀色松焦油，前景有松果。`
- Read-only handoff comparison: both handoffs record `altStatus: verified_from_actual_file` and `assetStatus: received_local_validated`. These localized alts intentionally describe the same actual cover in each locale; they replace the stale `null`/`pending_media` statement and align with the receipt-bound cover facts.

### Figure 1 — `/blog/illustrations/pine-tree-seed-to-tar.webp`

- Placement: after the object-mapping section and before Pine Cone acquisition (EN); after the object-mapping table and before planting (ZH).
- EN alt: `Explanatory illustration showing a Pine Cone, planted common-tree stages, a mature Pine with a Tapper, and Pine Tar; not a gameplay screenshot.`
- EN caption: `Pine Cone leads to a Pine Tree, and only a mature Pine enters the Tapper-to-Pine-Tar step. This is an explanatory illustration, not a gameplay screenshot; it does not show a fixed growth countdown.`
- ZH alt: `图示：松果对应松树；普通树长成成熟松树后，树液采集器产出松焦油。这是规则关系示意图，不是游戏截图。`
- ZH caption: `松果 → 普通松树 → 成熟松树上的树液采集器 → 松焦油。图中表达对象关系，不表示固定成长天数或收益排名。`

### Figure 2 — `/blog/illustrations/pine-tree-stage-four-neighbor.webp`

- Placement: after the eight-neighbor stage-4 diagnosis in both locales.
- EN alt: `Explanatory grid showing a Pine seedling at center, eight adjacent tiles, a mature neighboring tree blocking stage 4, and a corrected one-tile gap; not a gameplay screenshot.`
- EN caption: `The center Pine seedling is checked against all eight adjacent tiles. A mature neighboring tree blocks progress beyond stage 4; the separated example shows a practical gap, not a fruit-tree 3-by-3 clearance requirement.`
- ZH alt: `示意图：以未长大的松树苗为中心检查八个相邻格；成熟邻树会阻止它越过第 4 阶段，修正方案把树干错开一格。这不是游戏截图或物理碰撞框。`
- ZH caption: `先检查树苗周围八个相邻格；成熟邻树会卡住第 4 阶段。树干之间留一格是便于布局和排错的做法，不是果树 3×3 规则。`

## Original generation and conversion evidence

This section preserves the original 2026-09-22 generation evidence. It is provenance only; this reconciliation did not rerun generation, recompress, rename, replace, or otherwise alter any asset.

No paid or production service was called, no dependency was added, and no source image was copied from the repository. The source PNGs were generated by a temporary local Python/Pillow drawing script outside the repository, then removed from the repository scope; final public files are only the approved WebP/AVIF paths and this receipt.

```sh
python3 /tmp/create_pine_assets.py

cwebp -quiet -q 84 -m 6 -mt \
  /tmp/pine-tree-stardew-assets/pine-tree-stardew-cover.png \
  -o /tmp/pine-tree-stardew-assets/pine-tree-stardew-cover.webp

cwebp -quiet -q 79 -m 6 -mt \
  /tmp/pine-tree-stardew-assets/pine-tree-seed-to-tar.png \
  -o /tmp/pine-tree-stardew-assets/pine-tree-seed-to-tar.webp

cwebp -quiet -q 79 -m 6 -mt \
  /tmp/pine-tree-stardew-assets/pine-tree-stage-four-neighbor.png \
  -o /tmp/pine-tree-stardew-assets/pine-tree-stage-four-neighbor.webp
```

AVIF siblings were encoded with the installed Pillow 12.2.0 AVIF encoder (`quality=58`, `speed=6`) and copied beside the matching WebP. The temporary source files are not part of the repository deliverable.

## Original validation evidence

The following commands were recorded against the final repository files during original generation:

```sh
webpinfo public/blog/pine-tree-stardew-cover.webp
webpinfo public/blog/illustrations/pine-tree-seed-to-tar.webp
webpinfo public/blog/illustrations/pine-tree-stage-four-neighbor.webp
```

Each command reported all of the following (recorded exit code `0`):

- `Chunk VP8` (not `VP8L` or `VP8X`)
- `Width: 1672`
- `Height: 941`
- `Format: Lossy (1)`
- `No error detected.`

The AVIF siblings begin with `ftypavif`, decode successfully through Pillow as `AVIF`, and each decodes to `1672×941` RGB. The original Pillow metadata read of all six files passed (exit code `0`), and all WebP byte budgets are met by a wide margin.

## Reconciliation validation (read-only, current checkout)

The following checks were run for this repair. They only read the six current media files and the refreshed EN/ZH handoffs; no media, handoff, source, registry, test, or other document file was written by these checks.

| Check | Command / observed result | Exit code |
|---|---|---:|
| Six files non-empty | `test -s` for each approved WebP/AVIF path | `0` |
| Six-file bytes and hashes | `shasum -a 256` for the six paths; every line matched the table above | `0` |
| WebP headers and dimensions | `webpinfo` for the three WebP paths; each reported `Chunk VP8`, `1672×941`, `Format: Lossy (1)`, `No error detected.` | `0` |
| AVIF decode and dimensions | Pillow read of the three AVIF paths; each reported `AVIF`, RGB, `1672×941`, and the recorded byte/hash values | `0` |
| Six-file receipt assertions | Pillow/hash/byte/dimension/format assertions against this receipt's table | `0` |
| Handoff cover-alt comparison | Fenced-JSON read of `handoff-en.md` and `handoff-zh.md`; exact localized alts plus `verified_from_actual_file` / `received_local_validated` matched | `0` |
| Receipt whitespace | `awk '/[[:blank:]]$/{print NR ":" $0; bad=1} END{exit bad}' docs/blog-ops/pine-tree-stardew/G-assets-receipt.md` | `0` |
| Repository diff whitespace | `git diff --check` | `0` |

The current readback therefore confirms that all six receipt-bound paths, bytes, SHA-256 values, dimensions, and formats remain truthful. The current cover visual matches both localized handoff descriptions; the cover remains a header-recognition image, while the two figure alt/caption contracts above remain unchanged.

## Remaining blocked items / out-of-scope findings

- The EN and ZH handoffs remain `blocked` (`ready: false`, `frozen: false`). Their unresolved registry fields remain explicit: EN `author=null`, `featured=null`, `readTimeMinutes=null`; ZH `topic=null`, `author=null`, `featured=null`, `readTimeMinutes=null`. This receipt repair does not infer or fill those values.
- Source registry/identity, localized route/page binding, build, browser, deployment, and user review remain unresolved or `NOT_RUN`; this receipt does not claim page readiness or production delivery.
- `tests/assets/public-avif-assets.test.ts` and all other tests remain unmodified. Its repository-wide fixed WebP-count contract is a separate out-of-scope test boundary, not a reason to alter the six approved asset facts here.
- This reconciliation changed only `docs/blog-ops/pine-tree-stardew/G-assets-receipt.md`; it did not alter asset bytes, figure facts, locked bodies, handoffs, source, registry, tests, or other docs, and it performed no commit, push, deploy, or external write.
